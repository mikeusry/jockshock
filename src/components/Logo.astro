---
// JockShock wordmark — uses the lightning-bolt-O logo from public/.
// Inverted on dark backgrounds; default file is dark-on-light, so we use
// the tagged version which already has the right contrast.
---

<img
  src="/JockShock_Logo.png"
  alt="JockShock"
  class="h-7 w-auto sm:h-9"
  width="280"
  height="80"
/>
